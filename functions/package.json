{
    "name": "wedlist-functions",
    "type": "module",
    "private": true,
    "engines": {
        "node": ">=18"
    },
    "scripts": {
        "build": "echo noop",
        "deploy": "firebase deploy --only functions"
    },
    "dependencies": {
        "firebase-admin": "^12.5.0",
        "firebase-functions": "^5.0.1"
    }
}